#!/usr/bin/env bash
# tmux config checksum: {{ include "dot_tmux.conf" | sha256sum }}
set -euo pipefail

installer="${HOME}/.tmux/plugins/tpm/bin/install_plugins"
if [[ -x "${installer}" ]]; then
  "${installer}"
fi
