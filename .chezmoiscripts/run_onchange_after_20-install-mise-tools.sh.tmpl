#!/usr/bin/env bash
# mise config checksum: {{ include "dot_config/mise/config.toml" | sha256sum }}
set -euo pipefail

export PATH="${HOME}/.local/bin:${PATH}"

if ! command -v mise >/dev/null 2>&1; then
  printf 'mise is required but was not found in PATH\n' >&2
  exit 1
fi

mise install
